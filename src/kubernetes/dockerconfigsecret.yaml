apiVersion: v1
kind: Secret
metadata:
  name: dockerconfigjson
  namespace: cinemaabyss
data:
  # Пакеты GHCR public — auth не требуется. Ниже валидный пустой docker config.
  # Для private пакетов замените на base64 от своего ~/.docker/config.json с auth для ghcr.io.
  .dockerconfigjson: eyJhdXRocyI6e319
type: kubernetes.io/dockerconfigjson
